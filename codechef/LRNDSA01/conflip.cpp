#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int g;
        cin >> g;
        while (g--)
        {
            int I, N, Q;
            cin >> I >> N >> Q;
            int f[N];
            int ans = 0;
            if (I == Q)
            {
                cout << N / 2 << endl;
            }
            else
                cout << (N + 1) / 2 << endl;
        }
    }
}