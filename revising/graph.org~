* Graph Traversal
** Depth First Search (dfs)
#+NAME: dfs
#+begin_src cpp
  vector<int> adj[N];
  bool visited[N];
  void dfs(int s){
    if(visited[s]) return;
    visited[s] = true;
    // process node s here
    for(auto u: adj[s]){
      dfs(u);
    }
  }
#+end_src
