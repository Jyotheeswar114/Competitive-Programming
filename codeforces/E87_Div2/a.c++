#include <stdio.h>
using namespace std;
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        int a,b,c,d;
        cin>>a,b,c,d;
        if(b>=a)
        {
            printf("%d\n",b);
            return 0;
        }
        if((d-c) >= 0)
        {
            printf("-1\n");
            return 0;
        }
        printf("
    }
    return 0;
}
